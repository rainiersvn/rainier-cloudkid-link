User-agent: *
# Allow all resources for proper rendering
Allow: /
# If you truly need to keep SASS sources hidden (only if deployed), uncomment:
# Disallow: /sass/

# Avoid blocking CSS/JS/Fonts – critical for indexing
Sitemap: https://cloudkid.link/sitemap.xml